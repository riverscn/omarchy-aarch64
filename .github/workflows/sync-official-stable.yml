name: Sync official stable release

on:
  schedule:
    - cron: '5 */6 * * *'
  workflow_dispatch:

concurrency:
  group: aarch64-source-stable-sync
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          ref: main
          fetch-depth: 0

      - uses: actions/checkout@v7
        with:
          repository: riverscn/omarchy-pkgs-aarch64
          ref: master
          path: .test/omarchy-pkgs-aarch64

      - name: Install repository database tools
        run: sudo apt-get update && sudo apt-get install -y libarchive-tools lua5.4 ripgrep

      - name: Adapt the official stable source release
        id: stable
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          ./.github/scripts/sync-official-stable --output "$GITHUB_OUTPUT"

      - name: Test the adapted source
        env:
          LANG: C.UTF-8
          LC_ALL: C.UTF-8
          OMARCHY_PKGS_PATH: ${{ github.workspace }}/.test/omarchy-pkgs-aarch64
        run: |
          set -euo pipefail
          ./test/cli
          bash test/shell.d/aarch64-runtime-test.sh
          bash test/shell.d/firewall-config-test.sh
          bash test/shell.d/config-test.sh

      - name: Publish synchronized tags and source
        env:
          OFFICIAL_TAG: ${{ steps.stable.outputs.official_tag }}
          ADAPTED_TAG: ${{ steps.stable.outputs.adapted_tag }}
        run: git push --atomic origin HEAD:main "$OFFICIAL_TAG" "$ADAPTED_TAG"

      - name: Publish synchronized Releases
        env:
          GH_TOKEN: ${{ github.token }}
          OFFICIAL_TAG: ${{ steps.stable.outputs.official_tag }}
          ADAPTED_TAG: ${{ steps.stable.outputs.adapted_tag }}
          REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail

          publish_release() {
            local tag=$1 title=$2 notes=$3

            if gh release view "$tag" --repo "$REPOSITORY" >/dev/null 2>&1; then
              echo "Release already exists: $tag"
              return
            fi

            gh release create "$tag" \
              --repo "$REPOSITORY" \
              --verify-tag \
              --title "$title" \
              --notes "$notes"
          }

          publish_release \
            "$OFFICIAL_TAG" \
            "Omarchy ${OFFICIAL_TAG#v}" \
            "Synchronized from the upstream Omarchy release: https://github.com/basecamp/omarchy/releases/tag/${OFFICIAL_TAG}"
          publish_release \
            "$ADAPTED_TAG" \
            "Omarchy AArch64 ${ADAPTED_TAG#v}" \
            "Generic AArch64 VM adaptation of upstream ${OFFICIAL_TAG}."
